{
    "name": "example-app-photos",
    "version": "1.1.1",
    "private": true,
    "scripts": {
        "clean": "rm -rf .next",
        "dev": "node scripts/ensure-env.mjs && pnpm run clean && pnpm --filter @magic-link-sso/nextjs build && exec next dev --webpack -p 5001",
        "build": "node scripts/ensure-env.mjs && pnpm run clean && pnpm --filter @magic-link-sso/nextjs build && next build --webpack",
        "format": "oxfmt package.json tsconfig.json next.config.mjs postcss.config.mjs tailwind.config.ts vitest.config.ts src tests",
        "format:check": "oxfmt --check package.json tsconfig.json next.config.mjs postcss.config.mjs tailwind.config.ts vitest.config.ts src tests",
        "lint": "oxlint src tests vitest.config.ts",
        "start": "node scripts/ensure-env.mjs && next start -p 5001",
        "test": "vitest run",
        "typecheck": "node scripts/ensure-env.mjs && next typegen && tsc -p tsconfig.json --noEmit"
    },
    "dependencies": {
        "@magic-link-sso/config-core": "workspace:*",
        "@magic-link-sso/nextjs": "workspace:*",
        "next": "^16.2.9",
        "react": "^19.2.7",
        "react-dom": "^19.2.7"
    },
    "devDependencies": {
        "@tailwindcss/postcss": "^4.3.1",
        "@types/node": "^25.9.3",
        "@types/react": "^19.2.17",
        "@types/react-dom": "^19.2.3",
        "postcss": "^8.5.15",
        "tailwindcss": "^4.3.1",
        "typescript": "^6.0.3",
        "vitest": "^4.1.9"
    },
    "engines": {
        "node": ">=24.15.0"
    }
}
